# y+m 博客 —— 使用维护指南

> 博客已搭建完成 ✅ 本文档教你**怎么用**，不是怎么搭。

---

## 一、现状一览（都已配好，不用动）

| 项 | 值 |
|----|-----|
| 博客地址 | **https://yaojiaming-cn.github.io/** |
| 博客名 | y+m |
| 作者 | 姚嘉明 |
| 主题 | beautiful-jekyll（干净、支持标签/深浅色/搜索） |
| GitHub 仓库 | `YaoJiaming-cn/YaoJiaming-cn.github.io`（公开） |
| 本地位置 | `D:\projects\my-blog` |
| 文章文件夹 | `D:\projects\my-blog\_posts\` |
| Git 推送 | 已配置（gh 凭据），直接 `git push` 即可 |

原理：仓库里的 Markdown 文章会被 GitHub 自动编译成网页，约 1 分钟上线。你只负责写，剩下的交给 GitHub。

---

## 二、发文章（两种方式任选）

### 方式 1：GitHub 网页写（推荐，不用装任何东西）

1. 浏览器打开仓库：https://github.com/YaoJiaming-cn/YaoJiaming-cn.github.io
2. 进入 **`_posts/`** 文件夹 → **Add file → Create new file**
3. 文件名写 **`2026-08-18-标题.md`**（日期用当天，格式 `年-月-日`，文件名里的日期=发布日期）
4. 内容开头照抄这个格式：

```markdown
---
layout: post
title: "文章标题"
subtitle: "一句话副标题"
tags: [随笔]
---

正文在这里，用 Markdown 写。
```

5. 点 **Commit changes** → 等约 1 分钟 → 刷新博客就能看到新文章

### 方式 2：本地写（写完推到 GitHub）

1. 在 `D:\projects\my-blog\_posts\` 里新建 `2026-08-18-标题.md`（格式同上）；
2. 在 `D:\projects\my-blog` 目录打开命令行，执行：

```bat
git add .
git commit -m "post: 标题"
git push
```

（Git 凭据已配好，push 不会问你账号密码。）

### 也可以让我代劳

写好的 Markdown 丢给我（在 DSH 里发我），我帮你放到 `_posts/`、提交、推送，一步到位。

---

## 三、改站点配置

编辑 `D:\projects\my-blog\_config.yml`（或仓库里的同名文件）：

- `title:` 博客名
- `author:` 作者名
- `description:` 一句话简介
- `github_username: YaoJiaming-cn` —— 取消注释后，页头会显示 GitHub 链接
- `avatar:` —— 放头像：把图片传到仓库 `assets/img/` 下，再取消这行注释

改完按第二节方式 2 推送，等 1 分钟生效。

---

## 四、常见问题

| 问题 | 解决 |
|------|------|
| 打开博客显示 404 | 刚推送完在构建，等 1~3 分钟刷新；还不行就去仓库 **Actions** 页看构建日志 |
| 文章不显示 | 检查文件名是否是 `年份-月份-日期-标题.md`，且文章顶部有 `---` 开头的配置 |
| 改了配置没生效 | 等 1 分钟让 GitHub 重新构建；或看 Actions 是否有报错 |
| 想换头像 | 图片放 `assets/img/`，在 `_config.yml` 取消 `avatar:` 注释 |
| 本地 push 报"找不到命令" | 用管理员装 git，或直接用方式 1 在网页写 |

---

## 五、小知识

- 博客 = 一个公开 GitHub 仓库，文章就是仓库里的 Markdown 文件，天然备份、可迁移；
- GitHub Pages 免费、无限流量；
- 以后想用自定义域名（如 `blog.yjm.com`）也可以，设置里绑定即可。
