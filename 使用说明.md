# 用 GitHub Pages 搭个人博客 —— 完整步骤

> 主题：beautiful-jekyll（干净、支持标签/深浅色/关于页）
> 原理：GitHub 免费托管 + 自动编译。你只负责写 Markdown，剩下交给 GitHub。

---

## 第一步：注册 GitHub（已有账号可跳过）

1. 打开 https://github.com 注册一个账号（免费），**记下你的用户名**（英文，比如 `zhangsan`）。

## 第二步：创建仓库（一次性的）

1. 登录 GitHub，点右上角 **+** → **New repository**；
2. **Repository name** 必须填：**`你的用户名.github.io`**
   （例如用户名叫 `zhangsan`，就填 `zhangsan.github.io`）
3. **Public**（公开）；
4. 其他不用动，点 **Create repository**。

> 这个特殊名字 = GitHub 自动把这个仓库变成你的博客网站，地址就是 `https://你的用户名.github.io`。

## 第三步：把本文件夹的文件上传到仓库

打开这个 `my-blog` 文件夹，里面有：

```
_config.yml         ← 站点配置（改标题用）
index.md            ← 首页
aboutme.md          ← 关于我
_posts/             ← 放文章的文件夹
  2026-08-18-welcome-to-my-blog.md   ← 示例文章
```

**上传方式（二选一）：**

- **方式 A（推荐，不用装 git）**：在刚建好的仓库页面点 **Add file → Upload files**，把上面这些文件/文件夹**拖进页面**，然后点 **Commit changes**；
- **方式 B（命令行）**：
  ```bat
  cd /d D:\workspace\DSH\my-blog
  git init
  git add .
  git commit -m "init blog"
  git branch -M main
  git remote add origin https://github.com/你的用户名/你的用户名.github.io.git
  git push -u origin main
  ```

## 第四步：改你的站点配置

在 GitHub 仓库里点开 **`_config.yml`** → 点 **✏️ 编辑**：

- `title: 我的博客` → 改成你的博客名；
- `author: 你的名字` → 改成你的名字；
- `description: ...` → 改成一句话简介；
- （可选）取消 `url:` 那行的注释，填 `https://你的用户名.github.io`；
- 点 **Commit changes** 保存。

## 第五步：等部署，访问

1. 去仓库页 **Settings → Pages**，把 **Source** 设为 **Deploy from a branch → main → / (root)**，Save；
2. 等 1~3 分钟（第一次要久一点），打开 **`https://你的用户名.github.io`** 就能看到你的博客了！

---

## 以后怎么发文章（两种方式）

**方式 1：GitHub 网页写（最简单，不用装任何东西）**
1. 仓库里进 **`_posts/`** 文件夹 → **Add file → Create new file**；
2. 文件名写 **`2026-08-18-标题.md`**（日期必须是今天或未来，格式 `年-月-日`）；
3. 内容开头抄这个格式：
   ```markdown
   ---
   layout: post
   title: "文章标题"
   subtitle: "一句话副标题"
   tags: [随笔]
   ---
   
   正文在这里，用 Markdown 写。
   ```
4. 点 **Commit changes**，约 1 分钟后文章上线。

**方式 2：本地写**
- 在 `_posts/` 里新建 Markdown 文件，写完用 git push（或拖拽上传）。

---

## 常见问题

| 问题 | 解决 |
|------|------|
| 打开 `https://你的用户名.github.io` 显示 404 | 仓库名没取对（必须 `<用户名>.github.io`），或 Settings→Pages 没设 Source，或还在构建（等几分钟） |
| 文章不显示 | 检查 `_posts/` 里的文件名格式是否是 `年份-月份-日期-标题.md`，且文章顶部有 `---` 开头的配置 |
| 改了配置没生效 | 正常，等 1 分钟让 GitHub 重新构建；或者去仓库 **Actions** 标签页看构建是否成功、有没有报错 |
| 想换头像 | 把图片上传到 `assets/img/`，然后在 `_config.yml` 里取消 `avatar:` 的注释 |

---

## 小知识

- 博客本身 = 一个公开的 GitHub 仓库，你的文章就是仓库里的 Markdown 文件，随时能备份/迁移；
- GitHub Pages 免费、无限流量，域名就是 `https://你的用户名.github.io`；
- 以后想要自定义域名（比如 `blog.myname.com`）也可以，设置里绑定即可（以后再说）。
